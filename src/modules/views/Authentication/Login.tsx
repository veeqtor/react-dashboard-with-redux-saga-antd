import * as React from 'react';
import styled from 'styled-components';

const Login = (): React.ReactElement => {
  return <Login.Wrapper>Login Page</Login.Wrapper>;
};

Login.Wrapper = styled.div`
  padding: 10px;
  background: #fff;
`;

export default Login;
